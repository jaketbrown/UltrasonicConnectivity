<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.cmsc436.honeyguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".WelcomePage"
            android:label="@string/welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".defaultPiece"
            android:label="@string/defaultPiece" />
        <activity
            android:name=".artPiece"
            android:label="@string/artPiece" />

        <activity
            android:name=".MainActivity"
            android:label="@string/add_temp"/>
        <activity
            android:name=".ListPiecesView"
            android:label="@string/list_pieces" />
        <activity
            android:name=".ShowPiece"
            android:label="@string/show_piece" />
    </application>

</manifest>